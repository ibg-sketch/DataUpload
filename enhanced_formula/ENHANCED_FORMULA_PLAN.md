# 🚀 Enhanced Formula Implementation Plan
## Улучшенная формула v1 (12 факторов) - Дорожная карта

---

## 📊 ТЕКУЩИЙ СТАТУС

### ✅ ГОТОВО:
1. **Инфраструктура логирования** - сигналы записываются с 21 полем:
   - `timestamp_sent`, `timestamp_checked`, `symbol`, `verdict`, `confidence`
   - `entry_price`, `target_min`, `target_max`, `duration_minutes`
   - `result`, `highest_reached`, `lowest_reached`, `final_price`
   - `profit_pct`, `duration_actual`, `market_strength`
   - **🆕 rsi, ema_short, ema_long, adx, funding_rate** (новые!)

2. **Скрипт обучения модели** - `train_enhanced_formula.py`:
   - Множественная линейная регрессия
   - Cross-validation
   - Feature importance анализ
   - Автоматическое сохранение модели

3. **Модуль интеграции** - `enhanced_formula_integration.py`:
   - Загрузка обученной модели
   - Предсказание движения цены
   - Расчёт таргетов на основе ML

4. **Анализ корреляций** - `correlation_last_hour.py`:
   - Текущая формула: **R² = 0.014 (1.4%)**
   - Цель улучшения: **R² = 0.2-0.4 (20-40%)**
   - Win rate: **100%** (направление определяется идеально)

### ⏳ В ПРОЦЕССЕ:
- **Накопление данных**: 0/50 сигналов с новыми полями
- **Ожидание**: 24-48 часов до начала обучения

---

## 📈 5-ШАГОВЫЙ ПЛАН РЕАЛИЗАЦИИ

### **ШАГ 1: СБОР ДАННЫХ** ⏳ (24-48 часов)

**Статус:** В процессе  
**Прогресс:** 0/50 сигналов

**Действия:**
- ✅ Бот логирует новые поля (RSI, EMA, ADX, Funding)
- ⏳ Дождаться накопления 50+ завершённых сигналов
- ⏳ Проверять прогресс: `wc -l effectiveness_log.csv`

**Критерий готовности:**
```bash
# Проверка количества сигналов с полными данными
awk -F',' '{if (NF == 21 && $17 != "") print}' effectiveness_log.csv | wc -l
# Должно быть >= 50
```

---

### **ШАГ 2: ОБУЧЕНИЕ МОДЕЛИ** (5 минут)

**Статус:** Ожидает данных  
**Файл:** `train_enhanced_formula.py`

**Команда запуска:**
```bash
python3 train_enhanced_formula.py
```

**Ожидаемый результат:**
- ✅ R² score: 0.2-0.4 (в 14-28 раз лучше текущих 0.014!)
- ✅ Файлы созданы:
  - `enhanced_formula_model.pkl` (обученная модель)
  - `enhanced_formula_metadata.json` (метаданные)

**Что делает скрипт:**
1. Загружает effectiveness_log.csv (21 столбец)
2. Фильтрует только завершённые сигналы (WIN/LOSS)
3. Извлекает 12 факторов:
   - Base (6): RSI, EMA_short, EMA_long, ADX, Funding, Market_Strength
   - Derived (6): EMA_cross, Price_momentum, + 4 из analysis_log
4. Обучает модель множественной регрессии
5. Валидирует (train/test split + cross-validation)
6. Сохраняет модель и коэффициенты

---

### **ШАГ 3: ИНТЕГРАЦИЯ В ОСНОВНОЙ КОД** (30 минут)

**Статус:** Готов к интеграции  
**Файлы:** `smart_signal.py`, `enhanced_formula_integration.py`

**Изменения в `smart_signal.py`:**

```python
# В начале файла добавить импорт
from enhanced_formula_integration import (
    calculate_enhanced_targets,
    get_model_status
)

# В функции calculate_price_targets() после текущих расчётов:
def calculate_price_targets(...):
    # Существующая логика (СОХРАНИТЬ как fallback)
    min_pct_old = ...  # текущий расчёт
    max_pct_old = ...  # текущий расчёт
    
    # НОВАЯ ЛОГИКА: попытка использовать enhanced model
    indicator_data = {
        'rsi': res.get('rsi'),
        'ema_short': res.get('ema_short'),
        'ema_long': res.get('ema_long'),
        'adx': res.get('adx'),
        'funding_rate': res.get('funding_rate'),
        'market_strength': multiplier,
        'price_momentum': 0  # TODO: calculate from klines
    }
    
    # Пробуем enhanced модель
    target_min_new, target_max_new, predicted_pct = calculate_enhanced_targets(
        current_price=last_close,
        confidence=confidence,
        indicator_data=indicator_data,
        verdict=verdict,
        fallback_targets=(min_pct_old, max_pct_old)
    )
    
    # Используем результат (модель сама делает fallback если не загружена)
    return (predicted_pct, max_pct_new, duration, move_str, 
            multiplier, strength_icon, strength_label, ttl_minutes, base_interval)
```

---

### **ШАГ 4: ТЕСТИРОВАНИЕ** (1-2 часа)

**Чеклист:**
- [ ] Модель загружается без ошибок
- [ ] Предсказания генерируются корректно
- [ ] Fallback работает если модель недоступна
- [ ] Логирование новых предсказаний
- [ ] A/B тест: старая vs новая формула

**Тестовый скрипт:**
```bash
# 1. Проверить статус модели
python3 enhanced_formula_integration.py

# 2. Перезапустить бот
# (модель автоматически загрузится)

# 3. Через 1 час проверить корреляцию
python3 correlation_last_hour.py

# 4. Сравнить R²:
#    Старая формула: R² ≈ 0.014
#    Новая формула: R² ≈ 0.2-0.4 (цель)
```

---

### **ШАГ 5: МОНИТОРИНГ И ОПТИМИЗАЦИЯ** (постоянно)

**Метрики для отслеживания:**
1. **R² score** - каждые 24 часа
2. **Win rate** - должен остаться ~70%+
3. **Target hit rate** - сколько таргетов достигается
4. **Prediction error** - среднее отклонение

**Переобучение модели:**
```bash
# Каждые 7 дней или при накоплении 200+ новых сигналов
python3 train_enhanced_formula.py

# Модель автоматически обновится при следующем запуске бота
```

**Логи для мониторинга:**
```bash
# Проверить производительность
tail -50 effectiveness_log.csv

# Анализ корреляции
python3 correlation_last_hour.py
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
/home/runner/workspace/
├── smart_signal.py                     # Основной бот (ИНТЕГРАЦИЯ)
├── signal_tracker.py                   # Трекинг (✅ обновлён: 21 столбец)
├── main.py                             # Entry point (✅ обновлён: новые поля)
│
├── effectiveness_log.csv               # Данные (⏳ накапливаются)
│   └── 21 column: ..., rsi, ema_short, ema_long, adx, funding_rate
│
├── train_enhanced_formula.py          # ✅ Скрипт обучения
├── enhanced_formula_integration.py    # ✅ Модуль интеграции
├── correlation_last_hour.py           # ✅ Анализ корреляций
│
├── enhanced_formula_model.pkl         # ⏳ Обученная модель (после Шага 2)
├── enhanced_formula_metadata.json     # ⏳ Метаданные модели
│
└── ENHANCED_FORMULA_PLAN.md           # 📋 Этот файл
```

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Текущая формула (5 факторов):
```
R² = 0.014 (1.4%)
Корреляция = 0.12
Объясняет: 1.4% вариации цены
Недооценка: 3.6x (слишком консервативна)
```

### Улучшенная формула v1 (12 факторов):
```
R² = 0.2-0.4 (20-40%)           ← В 14-28 раз лучше! 🚀
Корреляция = 0.45-0.63
Объясняет: 20-40% вариации цены
Точность таргетов: ±1.2-1.5x
```

### Метрики успеха:
| Метрика | Текущая | Цель | Улучшение |
|---------|---------|------|-----------|
| **R²** | 0.014 | 0.2-0.4 | **14-28x** |
| **Корреляция** | 0.12 | 0.45-0.63 | **3.8-5.3x** |
| **Win Rate** | 100% | 70%+ | Сохранить |
| **Target Hit** | 100% | 80%+ | Более реалистично |

---

## 🔧 КОМАНДЫ БЫСТРОГО ДОСТУПА

```bash
# Проверить прогресс сбора данных
awk -F',' 'NF==21 && $17!=""' effectiveness_log.csv | wc -l

# Обучить модель (когда будет 50+ сигналов)
python3 train_enhanced_formula.py

# Проверить статус модели
python3 enhanced_formula_integration.py

# Анализ корреляции за последний час
python3 correlation_last_hour.py

# Перезапустить бот с новой моделью
# (модель загружается автоматически при старте)
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Fallback механизм**: Если модель не обучена или возникла ошибка, автоматически используется старая формула
2. **Переобучение**: Модель нужно переобучать каждые 7-14 дней или при накоплении 200+ новых сигналов
3. **A/B тест**: Первые 7 дней мониторить обе формулы параллельно
4. **Данные**: Минимум 50 сигналов для обучения, оптимально 100-200

---

## 📞 TROUBLESHOOTING

### Проблема: "NOT ENOUGH DATA"
**Решение:** Подождать 24-48 часов накопления сигналов

### Проблема: Модель не загружается
**Решение:** 
```bash
# Проверить наличие файла
ls -lh enhanced_formula_model.pkl

# Переобучить
python3 train_enhanced_formula.py
```

### Проблема: R² < 0.1 (слабая корреляция)
**Решение:** 
- Накопить больше данных (100-200 сигналов)
- Добавить факторы из analysis_log.csv
- Перейти к Продвинутой v2 (21 фактор)

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

**Немедленно:**
- ⏳ Ждать накопления 50+ сигналов (24-48 часов)

**Через 1-2 дня:**
- Запустить `train_enhanced_formula.py`
- Интегрировать в `smart_signal.py`
- Тестировать 24 часа

**Через неделю:**
- Проанализировать R² на реальных данных
- Если R² < 0.2 → добавить факторы из analysis_log
- Если R² > 0.3 → успех, продолжить мониторинг

**В будущем (опционально):**
- Продвинутая v2: 21+ фактор с взаимодействиями
- Экспертная v3: добавить макро индикаторы (BTC.D, Fear & Greed)
- Neural Network: LSTM для временных рядов

---

**Статус:** 🟡 Ожидание данных (0/50 сигналов)  
**Следующая проверка:** 2025-11-05 (через 24 часа)  
**ETA до запуска:** 24-48 часов
